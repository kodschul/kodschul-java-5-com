# Übungen: Die String-Klasse (Platzhalter)

**Status: Platzhalter.** Aufgabenformulierung nur anhand der IHV-Titel
(`3302_IHV (3).pdf` Kap. 9.2, 9.3); die offiziellen Aufgabentexte stehen erst
mit `3302_K09.pdf` zur Verfügung.

## 9.2 - Programm jederzeit während einer Runde beenden (obligatorisch)

Erweitern Sie den bisherigen `ArithmeticTrainer`-Code: prüfen Sie die
Benutzereingabe per `String`-Vergleich auf ein Abbruch-Kommando (z. B.
`"quit"`) und beenden Sie die Runde entsprechend.

## 9.3 - Beispielcode aus der JLS zu String-Gleichheit (optional)

Führen Sie ein Beispiel zu `==` vs. `.equals(...)` bei Strings aus der JLS
aus und identifizieren Sie die Fälle, in denen `==` unerwartet `true` oder
`false` liefert.
